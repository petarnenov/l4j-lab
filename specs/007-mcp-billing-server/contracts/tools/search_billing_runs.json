{
  "name" : "search_billing_runs",
  "title" : "Search billing runs",
  "description" : "Search billing runs for a firm by status, executing advisor, and start-date range. Returns one page of at most 20 runs. When more matches exist, the result carries an opaque cursor; pass it back as the cursor argument to get the next page. Results are limited to what the caller is entitled to see.",
  "inputSchema" : {
    "additionalProperties" : false,
    "properties" : {
      "advisor_id" : {
        "description" : "Optional. Only runs executed by this advisor.",
        "type" : "string"
      },
      "cursor" : {
        "description" : "Opaque cursor from a previous result. Do not construct or modify it.",
        "type" : "string"
      },
      "firm_id" : {
        "description" : "Firm whose runs to search.",
        "type" : "string"
      },
      "page_size" : {
        "description" : "Runs per page, from 1 to 20. Defaults to 20.",
        "default" : 20,
        "maximum" : 20,
        "minimum" : 1,
        "type" : "integer"
      },
      "started_from" : {
        "description" : "Optional. Only runs started on or after this date.",
        "format" : "date",
        "type" : "string"
      },
      "started_to" : {
        "description" : "Optional. Only runs started on or before this date.",
        "format" : "date",
        "type" : "string"
      },
      "status" : {
        "description" : "Optional. Only runs in this status.",
        "enum" : [ "PENDING", "RUNNING", "COMPLETED", "FAILED", "CANCELED" ],
        "type" : "string"
      }
    },
    "required" : [ "firm_id" ],
    "type" : "object"
  },
  "outputSchema" : {
    "title" : "BillingRunSearchResult",
    "description" : "One page of billing runs matching a search, with a cursor for the next.",
    "$id" : "http://localhost:8080/schemas/billing-run-search-result.schema.json",
    "$schema" : "https://json-schema.org/draft/2020-12/schema",
    "properties" : {
      "next_cursor" : {
        "type" : "string"
      },
      "refine_hint" : {
        "type" : "string"
      },
      "runs" : {
        "items" : {
          "title" : "BillingRunSummary",
          "description" : "One run in a search result.\n\n <p>Narrower than the status result on purpose: a search is for choosing which run to open, and\n phase counts and failure reasons are noise until one has been chosen.",
          "properties" : {
            "executed_by_advisor_id" : {
              "type" : "string"
            },
            "firm_id" : {
              "type" : "string"
            },
            "run_id" : {
              "type" : "string"
            },
            "started_at" : {
              "type" : "string"
            },
            "status" : {
              "description" : "The state a billing run is in.\n\n <p>Kept caller-facing on purpose: this Javadoc becomes the enum's {@code description} in the\n generated schema, so it is read by models, not only by people.",
              "enum" : [ "PENDING", "RUNNING", "COMPLETED", "FAILED", "CANCELED" ],
              "type" : "string"
            }
          },
          "type" : "object"
        },
        "type" : "array"
      },
      "total_match_count" : {
        "type" : "integer"
      },
      "truncated" : {
        "type" : "boolean"
      }
    },
    "required" : [ "total_match_count", "truncated" ],
    "type" : "object"
  },
  "annotations" : {
    "title" : "",
    "destructiveHint" : false,
    "idempotentHint" : true,
    "openWorldHint" : true,
    "readOnlyHint" : true,
    "returnDirect" : false
  }
}
